import { Vector3 } from "three";
export default class Projectile {
    constructor(velocity, size, color) {   

     this.size = size;  
     this.velocity = velocity;
     this.color = color;  

      
    }
  }